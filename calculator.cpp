#include "calculator.h"
// something here
int Calculator::Add (double a, double b)
{
	return a + b + 0.5;
}
// Verbovska Eva IO-03
int Calculator::Sub (double a, double b)
{
    return Add (a, -b);
}
// lab for ak-2
